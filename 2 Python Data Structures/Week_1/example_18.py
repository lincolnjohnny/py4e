# Prefixes

line = 'Please have a nice day.'

# verify if the line starts with the given parameter ('Please')
print(line.startswith('Please'))

# verify if the line starts with the given parameter ('p')
print(line.startswith('p'))